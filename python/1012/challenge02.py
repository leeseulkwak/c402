number=input("정수를 입력해주세요>")
number=int(number)

if number % 2 == 0: #2로 나눈 나머지가 0이면
   # print("{}ok".format(number))
   print(f"{number} 는 2로 나누어 떨어지는 숫자입니다.")
else:
   # print("{}로 나누어 떨어 지는 숫자가 아닙니다".format(number))
   print(f"{number} 는 2로 나누어 떨어지는 숫자가 아닙니다.")

if number % 3 == 0:
   # print("{}ok".format(number))
   print(f"{number} 는 3로 나누어 떨어지는 숫자입니다.")
else:
   # print("{}로 나누어 떨어 지는 숫자가 아닙니다".format(number))
   print(f"{number} 는 3로 나누어 떨어지는 숫자가 아닙니다.")

if number % 4 == 0:
   # print("{}ok".format(number))
    print(f"{number} 는 4로 나누어 떨어지는 숫자입니다.")
else:
   # print("{}로 나누어 떨어 지는 숫자가 아닙니다".format(number))
   print(f"{number} 는 4로 나누어 떨어지는 숫자가 아닙니다.")

if number % 5 == 0:
   # print("{}ok".format(number))
   print(f"{number} 는 5로 나누어 떨어지는 숫자입니다.")
else:
   # print("{}로 나누어 떨어 지는 숫자가 아닙니다".format(number))
   print(f"{number} 는 5로 나누어 떨어지는 숫자가 아닙니다.")